# 🚀 Пошаговая инструкция: Развертывание сайта на GitHub Pages

Это пошаговое руководство для новичков по созданию и развертыванию сайта на GitHub Pages.

## Шаг 1: Проверка файлов

Убедитесь, что у вас есть файл `index.html` в корне репозитория:
```
exply-dev-public-docs/
  ├── index.html          ← главная страница
  ├── api/
  ├── docs/
  └── README.md
```

## Шаг 2: Настройка GitHub Pages в репозитории

### 2.1. Откройте настройки репозитория

1. Перейдите на GitHub: https://github.com/exply-dev/exply-dev-public-docs
2. Нажмите на вкладку **"Settings"** (Настройки) в верхнем меню репозитория

### 2.2. Найдите раздел Pages

1. В левом боковом меню найдите раздел **"Pages"** (внизу списка)
2. Или перейдите напрямую: https://github.com/exply-dev/exply-dev-public-docs/settings/pages

### 2.3. Настройте источник

1. В разделе **"Source"** (Источник):
   - Выберите **"Deploy from a branch"** (Развернуть из ветки)
   - Выберите ветку: **`main`** (или `master`, если у вас такая)
   - Выберите папку: **`/ (root)`** (корень)
2. Нажмите **"Save"** (Сохранить)

### 2.4. Дождитесь публикации

- GitHub начнет процесс публикации (занимает 1-2 минуты)
- Вы увидите сообщение: *"Your site is ready to be published"* или *"Your site is published at..."*
- Ваш сайт будет доступен по адресу:
  ```
  https://exply-dev.github.io/exply-dev-public-docs/
  ```

## Шаг 3: Настройка кастомного домена (опционально)

Если у вас есть свой домен (например, `exply.dev`), вы можете подключить его к GitHub Pages.

### 3.1. Создайте файл CNAME

1. В корне репозитория создайте файл `CNAME` (без расширения!)
2. В файле напишите только ваш домен:
   ```
   exply.dev
   ```
   (или `www.exply.dev`, если хотите использовать поддомен)

3. Сохраните и закоммитьте:
   ```bash
   git add CNAME
   git commit -m "Add custom domain"
   git push origin main
   ```

### 3.2. Настройте DNS записи у регистратора домена

Вы должны настроить DNS записи у компании, где вы регистрировали домен (например, GoDaddy, Namecheap, etc.).

#### Вариант A: Использовать основной домен (exply.dev)

Добавьте DNS записи:

1. **A записи** (4 штуки):
   ```
   @    A    185.199.108.153
   @    A    185.199.109.153
   @    A    185.199.110.153
   @    A    185.199.111.153
   ```

2. Или используйте **CNAME запись**:
   ```
   @    CNAME    exply-dev.github.io.
   ```
   ⚠️ Не все регистраторы поддерживают CNAME для корневого домена (@)

#### Вариант B: Использовать поддомен (www.exply.dev)

Добавьте **CNAME запись**:
```
www    CNAME    exply-dev.github.io.
```

**Рекомендуется:** Использовать вариант B (www), он проще в настройке.

### 3.3. Обновите настройки в GitHub

1. Вернитесь в **Settings → Pages**
2. В разделе **"Custom domain"** введите ваш домен: `exply.dev` или `www.exply.dev`
3. Убедитесь, что стоит галочка **"Enforce HTTPS"** (Принудительно использовать HTTPS)
4. Сохраните

### 3.4. Дождитесь распространения DNS

- DNS изменения могут занять от нескольких минут до 48 часов
- Проверить можно через: https://www.whatsmydns.net/
- Введите ваш домен и проверьте, что он указывает на `185.199.108.153` или `exply-dev.github.io`

### 3.5. Проверка работы

После настройки DNS:
1. Подождите 5-10 минут
2. Откройте ваш домен в браузере: `https://exply.dev`
3. Если видите ваш сайт — всё готово! ✅

## Шаг 4: Обновление сайта

После настройки, чтобы обновить сайт:

1. Внесите изменения в файлы (например, `index.html`)
2. Закоммитьте и запушьте:
   ```bash
   git add .
   git commit -m "Update website"
   git push origin main
   ```
3. GitHub автоматически обновит сайт через 1-2 минуты

## Шаг 5: Включение HTTPS (автоматически)

- GitHub Pages автоматически предоставляет бесплатный SSL сертификат
- После подключения домена, подождите несколько минут
- Затем включите **"Enforce HTTPS"** в настройках Pages

## 🔍 Проверка и тестирование

### Локальное тестирование

Чтобы проверить сайт локально перед публикацией:

1. Откройте `index.html` в браузере:
   ```bash
   open index.html  # macOS
   # или просто дважды кликните на файл
   ```

### Проверка после публикации

1. Проверьте URL сайта: `https://exply-dev.github.io/exply-dev-public-docs/`
2. Проверьте все ссылки
3. Проверьте мобильную версию (откройте DevTools → Toggle device toolbar)

## ❓ Частые проблемы

### Сайт не обновляется

- Подождите 2-3 минуты (GitHub обновляет страницы не мгновенно)
- Очистите кэш браузера (Ctrl+Shift+R или Cmd+Shift+R)

### Домен не работает

- Проверьте DNS записи через https://www.whatsmydns.net/
- Убедитесь, что в файле `CNAME` указан правильный домен
- Подождите до 48 часов для распространения DNS

### Ошибка 404

- Убедитесь, что файл `index.html` находится в корне репозитория
- Проверьте, что выбрана правильная ветка в настройках Pages

## 📚 Полезные ссылки

- [Официальная документация GitHub Pages](https://docs.github.com/en/pages)
- [Настройка кастомного домена](https://docs.github.com/en/pages/configuring-a-custom-domain-for-your-github-pages-site)
- [Проверка DNS](https://www.whatsmydns.net/)

---

**Готово!** Ваш сайт должен быть доступен через несколько минут после настройки GitHub Pages.

