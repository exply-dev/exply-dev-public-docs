# 🔧 Локальная работа с репозиторием

## Расположение

```
/Users/vlad/exply-dev/exply-dev-public-docs/
```

## Быстрая работа

### 1. Перейти в директорию
```bash
cd /Users/vlad/exply-dev/exply-dev-public-docs
```

### 2. Внести изменения
Редактируйте файлы (например, `index.html`, файлы в `docs/`, `api/`)

### 3. Проверить статус
```bash
git status
```

### 4. Добавить изменения
```bash
git add .
# или конкретный файл
git add index.html
```

### 5. Закоммитить
```bash
git commit -m "Описание изменений"
```

### 6. Отправить на GitHub
```bash
git push origin main
```

## GitHub Pages обновится автоматически

После `git push` GitHub Pages обновится через 1-2 минуты.

**URL сайта:**
- GitHub Pages: https://exply-dev.github.io/exply-dev-public-docs/
- Кастомный домен: https://exply.dev (если DNS настроен)

## Структура репозитория

```
exply-dev-public-docs/
├── index.html              # Главная страница сайта
├── CNAME                   # Кастомный домен
├── api/                    # OpenAPI схемы
│   └── exply-project-proposals-api.json
├── docs/                   # Документация
│   ├── oauth-setup.md
│   ├── gpt-actions-setup.md
│   └── ...
└── README.md               # Описание репозитория
```

## Полезные команды

### Просмотр истории
```bash
git log --oneline -10
```

### Посмотреть изменения
```bash
git diff
```

### Обновить с GitHub (если кто-то еще пушил)
```bash
git pull origin main
```

### Проверить сайт локально
```bash
# Открыть index.html в браузере
open index.html  # macOS
```

## Важно

- ✅ Всегда работайте из этой директории (`/Users/vlad/exply-dev/exply-dev-public-docs`)
- ✅ Не работайте из `/tmp/exply-dev-public-docs` (временная директория)
- ✅ Проверяйте изменения перед коммитом: `git diff`
- ✅ Пишите понятные commit messages

